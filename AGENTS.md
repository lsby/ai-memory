# lsby-ai-memory AI 编码指南

## 项目定位

这是一个可发布的、绑定 PGlite 的 TypeScript 库：

- 源代码位于 `src/`，公共 API 从 `src/index.ts` 导出。
- 构建产物同时提供 ESM、CommonJS 和 TypeScript 类型声明。
- `test/` 用于单元测试，测试应优先验证公共导出，而不是内部实现细节。
- 记忆存储固定使用 PGlite / pgvector；不要引入第二个数据库或把持久化抽象为应用级运行时逻辑。

## 开发工作流

- 使用 pnpm 安装依赖和运行脚本，尽可能不要使用 npm 或 yarn。
- 修改公共 API 时，同时检查 ESM、CommonJS 和类型声明是否仍然一致。
- 修改完成后，根据变更范围运行类型检查、单元测试和构建。
- 修改依赖时必须同步更新 `pnpm-lock.yaml`。
- `private: true` 是模板的安全保护，除非用户明确要求，不要删除或执行发布操作。
- 发布前必须显式确认包名、版本、导出入口和最终 npm 包内容。

## 代码风格

- 默认使用 TypeScript。
- 文件名使用英文，并使用短横线连接，不使用驼峰命名。
- 变量名、函数名、类名和方法名在中文能清楚表达含义时，尽可能使用中文。
- 所有函数都写返回类型，类成员都写访问修饰符。
- 使用 `let`，不要使用 `var` 或 `const`。
- 使用完整、严格的条件判断，不省略真值、空值或 `null` 判断。
- 尽可能使用 `type` 而不是 `interface`。
- 尽可能不要使用 `any`、非空断言 `!` 和不安全的 `as` 强制类型转换。
- 访问数组下标时，总是考虑索引越界和 `undefined`。
- 不要使用浮动的 Promise，不要通过 `void` 隐藏未处理的异步错误。
- 对联合类型使用 `switch` 完整处理各个分支，不要使用无法保证穷尽性的连续判断。
- 不要使用 `eslint-disable max-lines` 绕过行数限制。
- 修改代码时不要删除已有的有效注释或空值断言，除非它们确实已经不再需要。
- 在不影响可读性和逻辑的前提下，减少不必要的中间变量和换行。

## 库设计约束

- 公共 API 必须通过 `src/index.ts` 明确导出，不能依赖消费者直接引用 `src` 内部文件。
- 不要把测试辅助代码、构建脚本或内部实现导出为公共 API。
- 新增子路径导出时，必须同步更新 `package.json` 的 `exports` 和对应类型入口。
- 不要在库代码中写死具体应用名称、路径、端口、环境变量或部署方式。
- 优先保持运行时依赖最小化；仅为构建、测试和开发需要的包放入 `devDependencies`。

## 测试

- 使用 Vitest。
- 测试应覆盖正常行为、边界条件和错误行为（如果公共 API 存在这些情况）。
- 优先通过公共入口导入被测内容，例如从 `src/index.ts` 导入，而不是绕过入口访问内部文件。
- 测试名称要表达行为，不要使用无意义的 `test`、`case1` 等名称。
- 不要为了提高覆盖率测试实现细节；重构内部实现时，测试应尽量保持稳定。

## AI 协作原则

- 先阅读相关源码、配置和现有测试，再提出或实施修改。
- 不要为了修复一个局部问题顺带改动无关文件或升级无关依赖。
- 遇到旧代码兼容问题时，不要擅自添加兼容分支；先向用户确认兼容范围。
- 涉及发布、删除文件、修改公共 API 或大范围依赖升级时，必须明确说明影响。
- 完成后简要报告修改内容、验证结果，以及仍受环境限制未能执行的检查。
